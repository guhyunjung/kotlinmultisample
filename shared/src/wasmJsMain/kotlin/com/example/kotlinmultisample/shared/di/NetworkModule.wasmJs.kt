package com.example.kotlinmultisample.shared.di

// Previously this file contained an `actual val networkModule` with a TODO implementation.
// The wasmJs-specific implementation has been moved to `NetworkModule.kt` in the same
// source set to provide a concrete (empty) module. Keep this file as a stub to avoid
// accidental re-introduction of a second `actual` declaration.

// No `actual` declaration here to prevent conflicting declarations.
